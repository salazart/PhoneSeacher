package com.salazart.db;

public class UsePhone {

	public static void main(String[] args) {

		/*
		 * IPhone phoneService = new PhoneService();
		 * 
		 * Phone phone = new Phone(); phone.setUserId(23);
		 * phone.setPhone("32432535342");
		 * 
		 * phoneService.addPhone(phone); phoneService.addPhone(phone);
		 * phoneService.addPhone(phone);
		 * 
		 * phoneService.printAllPhones();
		 * 
		 * phoneService.dropPhone();
		 */
		
		/*UserService userService = new UserService();
		
		PhoneService phoneService = new PhoneService();
		List<Phone> phones = phoneService.getAllPhones();
		
	    for (int i = 1; i < phones.size(); i++) {
	    	System.out.println(i);
	    	User user = userService.getUserById(phones.get(i).getUserId());
	    	System.out.println(user.getId() + "  " + user.getSocialId());
	    	phoneService.updatePhone(phones.get(i), user.getSocialId());
		}*/


	}

}
